/* Tentativas, número sorteado, número adivinhado */
var t, n, a
  
    a = parseInt(prompt("Adivinhe o número: "))

    /* Lógica */
    function sortear() {

        /* Limite máximo e minimo */
        const min = 0;
        const max = 10;

        const sort = Math.floor((Math.random() * max) + min);

    }




